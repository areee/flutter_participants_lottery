name: flutter_participants_lottery
description: A participants lottery app

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.4
  circular_countdown_timer: ^0.2.0
  shared_preferences: ^2.0.12
  emoji_alert:  1.1.0
  # audioplayers: ^0.19.1 # Do not use for now (to support also Windows desktop)
  envify: any
  google_fonts: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  envify_generator: any
  build_runner: any

flutter:
  uses-material-design: true

  # Do not use for now (to support also Windows desktop)
  # assets:
  #   - assets/aika_loppui.mp3
  #   - assets/arvonta_kaynnissa.mp3
